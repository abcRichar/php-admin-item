# 需求修改

1. 小程序用户设置页面需要加一个状态的开关编辑功能，如果是关-不能登录小程序，是开-可以登录小程序，这个功能下级代理商也可以设置。
2. 小程序用户设置页面需要加一个更新任务开关，如果是关-不能更新任务，是开-可以更新任务，就是小程序用户做完任务后，要开启第二天新任务的时候，就必须要后台点击确认开启更新任务，那边才能重新开始。这个任务就是小程序抢单那一块，这个功能下级代理商也可以设置。


注意：
1. 禁止修改以外的代码
2. 禁止直接删除代码
3. 禁止就改小程序接口返回字段
