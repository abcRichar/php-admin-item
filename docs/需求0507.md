# 新增需求描述

- 后台需求
1.新增一个菜单：小程序系统管理（要存储到数据库）
  - 可以设置充值地址（这个目前是在小程序用户设置里面，现在需要移到小程序系统管理里面）、设置固定佣金比例
2.新增一个菜单：派单模式管理（要存储到数据库）
  - 用于设置小程序用户设置页面里面的派单配置能直接下拉框选择，然后小程序用户设置那边到时候直接拿派单模式管理的数据进行下拉选择。
  - 字段：模版名称、派单订单、佣金比例、固定佣金、派单金额。（这些就是和小程序用户设置页面里面的派单配置一样）
3.修改小程序用户设置：
  - 账号密码、资金密码能直接在后台修改，留空则不修改。
  - 派单模式下拉框，下拉列表从派单模式管理里面拿。然后关联上派单模式id。
  - 模版名称、派单订单、佣金比例、固定佣金、派单金额这四字段去掉，直接是派单模式下拉框，下拉列表从派单模式管理里面拿。然后关联上派单模式id。
  - 增加两个字段：账号密码、资金密码。能直接在后台修改密码，留空则不修改。

【必须要求】
1. 禁止直接删除全部代码然后又生成。
2. 禁止直接修改数据库表结构。
3. 禁止修改需求以外的代码。
4. 小程序接口字段不准改换。
5. 需要sql执行的文件，请给我手动执行。