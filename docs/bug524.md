# bug修改

1. 当前有个非常严重的bug，/miniapp/rot_order/orderInfo这个的差额不对，如果我在后台设置了派单模式，然后只设置了差额，没有设置金额。然后我在后台充值了差额后，后台已经是0，小程序这个接口还有差额。这个是不对的。我需要你把这个差额和后台应该是一样的。复现：设置了派单模式，然后只设置了差额，没有设置金额。然后我在后台充值了差额后，后台已经是0，小程序这个接口还有差额。这个是不对的。

2. orderInfo这个里面有个user_balance和uinfo.balance字段，他们的值为什么不一样？不应该是一样的么？

【注意事项】

1. 不要修改小程序的返回字段。
2. 请注意当前的逻辑，不要改了之后之前的又不行。
3. 不要随意删除任何字段。
4. 注意不要影响之前的逻辑